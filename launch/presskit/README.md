# BLOCKWAVE press kit

Everything an outlet needs to cover BLOCKWAVE without asking a single follow-up question.

**Use freely.** All artwork here is original to this project and released under the same GPLv3 licence as the code. Reproduce it in coverage, crop it, resize it — no permission needed, no attribution required (though a link to https://github.com/nevercsof/blockwave is appreciated).

## Contents

| File | What it shows |
|---|---|
| `blockwave-craft-discovery.gif` | **The one to use.** An empty bench, blocks going down one at a time, then the moment a hidden recipe triggers: the toast appears, the patch name turns gold, the discovery counter ticks up. 832×456, small enough to embed anywhere. |
| `screenshots/01-craft-bench.png` | The CRAFT tab with a bench full of materials and an auto-generated patch name — the core screen. |
| `screenshots/02-recipe-discovered.png` | A recipe firing: gold name, toast, counter at 1/16. |
| `screenshots/03-tweak-full-synth.png` | The TWEAK page — proof there is a complete 67-parameter synth under the crafting layer. |
| `screenshots/04-preset-browser.png` | The preset browser with folders, favourites and search; each row shows the bench that made the preset. |
| `screenshots/05-per-block-mix.png` | The per-block MIX control across its states, blown up 4× so the pixel art is legible. |
| `logo/blockwave_wordmark_transparent_1x.png` (+ `_8x`) | Wordmark on transparency. |
| `logo/blockwave_wordmark_night_1x.png` (+ `_8x`) | Wordmark on the product's night background. |
| `logo/blockwave_icon_1x.png` (+ `_8x`) | Square icon, usable as an avatar. |
| `ONE_PAGER.md` | Facts sheet — every number and link on one page. |
| `DESCRIPTIONS.md` | Boilerplate at 25, 50, 100 and 250 words, ready to paste. |

**Pixel art note:** every image is pixel art at its native resolution. If you scale it, use nearest-neighbour — smooth interpolation turns it to mush. The `_8x` logo files are pre-scaled with nearest-neighbour for when you need something large.

## The short version, for a caption

> BLOCKWAVE — a free, open-source synthesizer where every sound is a square wave and patches are built by placing material blocks on a 3×3 grid. 128 presets, 16 secret recipes. macOS and Windows. GPLv3.

## Contact

Kirill Boyko — via GitHub issues or discussions at https://github.com/nevercsof/blockwave

## Legal

All artwork, the bitmap font and the sound design are original to this project. BLOCKWAVE is not affiliated with, endorsed by, or derived from any game or game publisher.
