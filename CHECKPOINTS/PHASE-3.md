# CHECKPOINT — Phase 3 (pixel UI: TWEAK tab + top bar + preset browser)

Date: 2026-08-02 · Verified by qa-runner. Verdict: **clear for checkpoint**. **RUN STOPS HERE for review** (covers Phases 2+3; PHASE-2.md committed earlier without a stop per architect instruction).

## Summary of work

Full pixel-art UI, all procedural (zero asset files — `art/` stays empty):
- **Canvas 832×456 @ 1x** (8-px grid), 1x/2x integer scaling via `AffineTransform` on a fixed content component — pixel-perfect (proof below). Scale persists in session state (`uiScale`, non-param).
- **PixelLookAndFeel**: 16-frame procedural stepped knobs cached as image strips (nearest-neighbour blits), bevelled block panels/buttons, LED toggles, pixel tooltips (≤3 words).
- **Original 3×5 bitmap font** drawn from scratch in code (A–Z, digits, punctuation) — no font files, no Mojang anything (brand grep: 0 hits).
- **TWEAK tab**: 14 bevelled panels, all **61 params reachable and readable** — 56 param cells (label/control/LCD readout with units: Hz/K, ms/s, dB, %, cents, st, synced LFO divisions incl. dotted) + 4 panel-title ON toggles + RAW in top bar; master_gain intentionally twice (MASTER cell + top-bar knob). FX section present (engine hookup = Phase 5 as planned).
- **Top bar**: BLOCK/WAVE logo (grass/ice), preset ◀ ▶, clickable preset name, PRESETS, SAVE, RAW LED, master knob, 1X/2X.
- **Preset browser**: category-grouped overlay (8 canonical categories), click-to-load, current highlighted, keyboard up/down/enter/esc + wheel; 12×12 recipe-icon slot reserved per row (Phase 4). **Save panel** with bitmap-font name entry + category cycler.
- **CRAFT tab**: default-selected per SPEC; original "under construction" pixel art (empty 3×3 grid + from-scratch mallet + caution stripes) until Phase 4.
- **`blockwave_screenshots`** console target renders every screen headless (`createComponentSnapshot`) — reproducible UI review without a host.

## DoD status

| DoD item | Status |
|---|---|
| Offscreen PNG screenshots of every screen attached | ✅ 7 PNGs in `CHECKPOINTS/screenshots/` (craft, tweak 1x+2x, top bar RAW off/on, browser, save panel), regenerated by qa-runner, all non-blank |
| All params reachable and readable | ✅ 61/61 IDs cross-checked cell-by-cell against `src/ParamSpec.h`, 0 missing |
| UI thread never blocks audio | ✅ static audit: 0 hits for mutex/locks/MessageManagerLock/sleep/direct engine access; 0 UI timers; APVTS attachments + 2 ParameterAttachment repaint watchers only |
| Resize/scale glitch-free | ✅ 2x = exact 2× (0 of 379 392 2×2 blocks non-uniform — perfect nearest-neighbour); only diff vs 1x is the scale-button label itself; setUiScale clamped 1..2 |
| pluginval s10 including GUI tests | ✅ VST3 + AU SUCCESS; Editor open cold/warm 227/207 ms, "Open editor whilst processing" and "Editor Automation" pass on both |

Build: 142/142 targets, **zero warnings in the entire log** (incl. JUCE). Tests: 58 + 184 = **242 checks green**, no regression; CPU worst case still 3.8 % @ 48k/512.

## Artifacts

- Screenshots: `CHECKPOINTS/screenshots/*.png` · regenerate: `./build/release/blockwave_screenshots_artefacts/Release/blockwave_screenshots CHECKPOINTS/screenshots`
- pluginval logs: `CHECKPOINTS/logs/` (`...VST3_20260802T033718...txt`, `...AudioUnit_20260802T033736...txt` + console logs)
- Builds: `build/release/BLOCKWAVE_artefacts/Release/{VST3,AU,Standalone}` · Windows VST3: CI artifact of this push
- New sources: `plugin/ui/` (PixelTheme, PixelFont, PixelLookAndFeel, ParamCells, BlockPanel, TweakTab, TopBar, PresetBrowser, CraftPlaceholder), `tools/screenshots/`, rewritten `plugin/PluginEditor.*`

## Open questions

1. AU pluginval prints informational `Current program is -1` (we expose presets via our own browser, not AU programs). Cosmetic; ship as-is or map presets to AU program list later?
2. Taste (Kirill): rows 2–3 of TWEAK carry 48/96-px inter-panel gaps (uneven param counts per SPEC section; cells kept uniform instead of stretching panels). OK, or should panels stretch to fill?
3. Taste (Kirill): bitmap font is caps-only → user preset names display uppercased. Acceptable for the product voice, or add lowercase glyphs?

## Proposals (out of scope, not built)

1. Editor open ~227 ms cold (JUCE image-strip generation) — could pre-render knob strips lazily per LookAndFeel instance if faster open is wanted.
2. Keyboard strip (SPEC: CRAFT tab) arrives with Phase 4 CRAFT build as planned.

## Human actions for this review

1. Look at `CHECKPOINTS/screenshots/` (or open the standalone) — pixel-art taste verdict, incl. open questions 2–3.
2. Play the plugin in FL Studio if desired (AU installed at `~/Library/Audio/Plug-Ins/Components/BLOCKWAVE.component`).
3. Architect: review PHASE-2.md (frozen 61 IDs, golden regeneration note) + this file; give the go for Phase 4 (CRAFT grid).
